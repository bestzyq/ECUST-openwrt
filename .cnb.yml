master:
  push:
    - name: sync
      runner:
        cpus: 1
      imports: https://cnb.cool/bestzyq/key/-/blob/main/env.yml
      stages:
        - name: sync to github
          image: tencentcom/git-sync
          settings:
            target_url: https://github.com/bestzyq/ECUST-openwrt.git
            auth_type: https
            username: ${GITHUB_USERNAME}
            password: ${GITHUB_ACCESS_TOKEN}
        - name: sync to gitee
          image: tencentcom/git-sync
          settings:
            target_url: https://gitee.com/bestzyq/ECUST-openwrt.git
            auth_type: https
            username: ${GITEE_USERNAME}
            password: ${GITEE_ACCESS_TOKEN}